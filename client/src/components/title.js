import React from 'react';
import "./title.scss"

const Title = () => {
    return (
        <div className="main-title">
            <span>FASTQ Quality Control</span>
        </div>
    );
};

export default Title;